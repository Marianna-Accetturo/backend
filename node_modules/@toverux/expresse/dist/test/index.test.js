"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const index = require("../src/index");
const sseHubMiddleware = require("../src/sse_hub_middleware");
const sseMiddleware = require("../src/sse_middleware");
describe('index', () => {
    describe('Public exports', () => {
        it('exports { sse } and also exports it as { default }', () => {
            chai_1.expect(index.sse).to.equal(sseMiddleware.sse);
            chai_1.expect(index.default).to.equal(sseMiddleware.sse);
        });
        it('exports { sseHub }', () => {
            chai_1.expect(index.sseHub).to.equal(sseHubMiddleware.sseHub);
        });
    });
});
